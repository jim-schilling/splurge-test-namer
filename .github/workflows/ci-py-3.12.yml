name: Python CI (3.12)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  call-reusable:
    uses: ./.github/workflows/ci-reusable.yml
    with:
      python-version: '3.12'
